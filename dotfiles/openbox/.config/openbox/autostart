# OPENBOX AUTOSTART
# remember to add a & at the end of EACH command
# appearance
/home/meme/bin/launch_polybar.sh &

# Check the GH version is does not work as expected
# https://github.com/himdel/hsetroot/tree/options
hsetroot -extend $BG_IMAGE &

# hide cursor
unclutter -idle 1.0 -root &



# cloud
/home/meme/bin/mega.sh &
dropbox start -i &

# anacron for backups or scheduled jobs
/usr/sbin/anacron -s -t $HOME/.anacron/etc/anacrontab -S $HOME/.anacron/spool &



# lock
xautolock -time 10 -locker "slock -m 'L' & (sleep .5 && xset dpms force off)" &

